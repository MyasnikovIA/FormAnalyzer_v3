----------------------------------------------------------------------------------------------------
ФОРМА: Forms/HospitPlanning/hospit_planning.frm
----------------------------------------------------------------------------------------------------
Базовая форма: C:\AppServ\www\5_mis_MEDDEV-151210\Forms\HospitPlanning\hospit_planning.frm
СТАТУС: ЧАСТИЧНО ПЕРЕОПРЕДЕЛЕНА

ЮЗЕРФОРМЫ:
     ===== UserFormsEgis3Novosib =====
        ЧАСТИЧНОЕ ПЕРЕОПРЕДЕЛЕНИЕ: C:\AppServ\www\5_mis_MEDDEV-151210\UserFormsEgis3Novosib\HospitPlanning\hospit_planning.d\hospit_planning.dfrm


SubForm:
     (не найдено)

Список вызываемых форм в JS:
     HospitPlanning/modals/graphics_add_edit.frm
     HospitPlanning/hosp_plan_kinds_edit.frm
     HospitPlanning/hosp_plan_deps.frm
     HospitPlanning/hosp_plan_pks.frm
     HospitPlanning/hosp_plan_mkbs.frm
     AccessRights/d_cse_accesses.frm
     HospPlan/hospplanperiod_edit.frm
     UniversalForms/period.frm
     Schedules/schedules_edit_hp.frm

Коды подключаемого AutoPopUp меню на форме:
        HOSP_PLAN_KINDS;
        HPK_PLANS;


Контекстное меню используемое на форме (ПКМ) – данные из OracleSQL:

├── name="pHOSP_PLAN_KINDS"
│   ├── "Обновить"
│   ├── "Добавить"
│   ├── "Редактировать"
│   ├── "Удалить"
│   ├── "Отделения"
│   ├── "Диагнозы"
│   ├── "Виды оплаты"
│   └── "Права записи"
│   
├── name="pHPK_PLANS"
│   ├── "Обновить"
│   ├── "Добавить"
│   ├── "Редактировать"
│   ├── "Удалить"
│   ├── "Назначить"
│   │   ├── "Ко всем дням недели"
│   │   ├── "День к периоду"
│   │   └── "Неделю в период"
│   └── "Копировать"
│       ├── "Ко всем дням недели"
│       ├── "День к периоду"
│       └── "Неделю в период"
│   
└── name="popupSchedule"
    ├── "Обновить"
    ├── "Просмотреть график"
    ├── "Добавить"
    ├── "Редактировать"
    └── "Удалить"

SQL ЗАПРОСЫ (12):
     (содержимое скрыто)

ИСПОЛЬЗУЕМЫЕ ТАБЛИЦЫ И ВЬЮХИ:
    D_V_HOSP_PLAN_KINDS
    D_V_HPK_PLANS
    D_V_HPK_SCHEDULE_BASE
    D_V_SCHEDULE_BASE

ТАБЛИЦЫ, ИСПОЛЬЗУЕМЫЕ ЧЕРЕЗ ВЬЮХИ (уникальные для этой формы):
    D_HOSP_PLAN_KINDS
    D_HPK_PLANS
    D_HPK_SCHEDULE
    D_SCHEDULE

ДЕТАЛЬНОЕ СОДЕРЖИМОЕ ВЬЮХ (таблицы):

  D_V_HOSP_PLAN_KINDS (Oracle: 868) (PostgreSQL: 368):
          D_HOSP_PLAN_KINDS (Oracle: 891) (PostgreSQL: 884)

  D_V_HPK_PLANS (Oracle: 30,05 тыс) (PostgreSQL: 26,83 тыс):
          D_HPK_PLANS (Oracle: 30,09 тыс) (PostgreSQL: 30,02 тыс)

  D_V_HPK_SCHEDULE_BASE (Oracle: 40) (PostgreSQL: 27):
          D_HPK_SCHEDULE (Oracle: 44) (PostgreSQL: 34)

  D_V_SCHEDULE_BASE (Oracle: 6,31 тыс) (PostgreSQL: 2,92 тыс):
          D_SCHEDULE (Oracle: 6,33 тыс) (PostgreSQL: 6,26 тыс)

ИСПОЛЬЗУЕМЫЕ ПАКЕТЫ И ФУНКЦИИ:
    D_PKG_CSE_ACCESSES.CHECK_RIGHT
    D_PKG_HPK_PLANS.SET_PLAN_FOR_DAY
    D_PKG_HPK_PLANS.SET_PLAN_FOR_WEEK
    D_PKG_HPK_PLANS.DEL
    D_PKG_HOSP_PLAN_KINDS.DEL
    D_PKG_URPRIVS.CHECK_BPPRIV
    D_PKG_CATALOGS.FIND_ROOT_CATALOG
    D_PKG_HPK_PLANS.COPY_PLAN_FOR_DAY
    D_PKG_HPK_PLANS.COPY_PLAN_FOR_WEEK
    D_PKG_HPK_SCHEDULE.DEL

РАЗОБРАТЬ АНАЛИТИКОМ:
    D_P_EXC

ПРОВЕРКА ПАКЕТОВ/ФУНКЦИЙ В PostgreSQL:

  D_PKG_CSE_ACCESSES.CHECK_RIGHT:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_cse_accesses.check_right(numeric, varchar, numeric, varchar, numeric, numeric, numeric)

  D_PKG_HPK_PLANS.SET_PLAN_FOR_DAY:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_hpk_plans.set_plan_for_day(numeric, numeric, varchar, timestamp, timestamp)

  D_PKG_HPK_PLANS.SET_PLAN_FOR_WEEK:
    Статус: СУЩЕСТВУЕТ (ПРЕДУПРЕЖДЕНИЯ)
    Сигнатура: d_pkg_hpk_plans.set_plan_for_week(numeric, numeric, timestamp, timestamp)
    ПРЕДУПРЕЖДЕНИЯ:
      warning extra:00000:46:FOR with integer loop variable:auto variable "nind" shadows a previously defined variable

  D_PKG_HPK_PLANS.DEL:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_hpk_plans.del(numeric, numeric)

  D_PKG_HOSP_PLAN_KINDS.DEL:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_hosp_plan_kinds.del(numeric, numeric)

  D_PKG_URPRIVS.CHECK_BPPRIV:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_urprivs.check_bppriv(numeric, varchar, numeric, numeric)

  D_PKG_CATALOGS.FIND_ROOT_CATALOG:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_catalogs.find_root_catalog(numeric, numeric, varchar, numeric)

  D_PKG_HPK_PLANS.COPY_PLAN_FOR_DAY:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_hpk_plans.copy_plan_for_day(numeric, numeric, varchar, timestamp, timestamp)

  D_PKG_HPK_PLANS.COPY_PLAN_FOR_WEEK:
    Статус: СУЩЕСТВУЕТ (ПРЕДУПРЕЖДЕНИЯ)
    Сигнатура: d_pkg_hpk_plans.copy_plan_for_week(numeric, numeric, timestamp, timestamp)
    ПРЕДУПРЕЖДЕНИЯ:
      warning extra:00000:45:FOR with integer loop variable:auto variable "nind" shadows a previously defined variable

  D_PKG_HPK_SCHEDULE.DEL:
    Статус: СУЩЕСТВУЕТ (OK)
    Сигнатура: d_pkg_hpk_schedule.del(numeric, numeric)

ПРОВЕРКА ПЕРВИЧНЫХ КЛЮЧЕЙ (Oracle vs PostgreSQL):

  D_HOSP_PLAN_KINDS:
    Статус: ОШИБКА: PK есть в Oracle, но отсутствует в PostgreSQL
    Oracle PK поля: ID

  D_HPK_PLANS:
    Статус: ОШИБКА: PK есть в Oracle, но отсутствует в PostgreSQL
    Oracle PK поля: ID

  D_HPK_SCHEDULE:
    Статус: ОШИБКА: PK есть в Oracle, но отсутствует в PostgreSQL
    Oracle PK поля: ID

  D_SCHEDULE:
    Статус: ОШИБКА: PK есть в Oracle, но отсутствует в PostgreSQL
    Oracle PK поля: ID

ПРОВЕРКА NOT NULL CONSTRAINT (Oracle vs PostgreSQL):

  D_HOSP_PLAN_KINDS:
    Колонка: HP_NAME
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN hp_name SET NOT NULL;

    Колонка: NUMB_GROUP
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN numb_group SET NOT NULL;

    Колонка: IS_OPER
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN is_oper SET NOT NULL;

    Колонка: HAS_PAYMENT_CONSTRAINTS
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN has_payment_constraints SET NOT NULL;

    Колонка: HAS_LIMITS
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN has_limits SET NOT NULL;

    Колонка: LPU
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN lpu SET NOT NULL;

    Колонка: HAS_MKB_CONSTRAINTS
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN has_mkb_constraints SET NOT NULL;

    Колонка: HP_CODE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN hp_code SET NOT NULL;

    Колонка: ID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN id SET NOT NULL;

    Колонка: JOURNAL_TYPE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN journal_type SET NOT NULL;

    Колонка: OPEN_DATE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN open_date SET NOT NULL;

    Колонка: CID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN cid SET NOT NULL;

    Колонка: HP_BLOCK
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hosp_plan_kinds ALTER COLUMN hp_block SET NOT NULL;

  D_HPK_PLANS:
    Колонка: LPU
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_plans ALTER COLUMN lpu SET NOT NULL;

    Колонка: PLAN_DATE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_plans ALTER COLUMN plan_date SET NOT NULL;

    Колонка: PID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_plans ALTER COLUMN pid SET NOT NULL;

    Колонка: ID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_plans ALTER COLUMN id SET NOT NULL;

    Колонка: CID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_plans ALTER COLUMN cid SET NOT NULL;

  D_HPK_SCHEDULE:
    Колонка: LPU
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN lpu SET NOT NULL;

    Колонка: DATE_BEGIN
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN date_begin SET NOT NULL;

    Колонка: ID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN id SET NOT NULL;

    Колонка: SCHEDULE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN schedule SET NOT NULL;

    Колонка: HOSP_PLAN_KINDS
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN hosp_plan_kinds SET NOT NULL;

    Колонка: DATE_CREATE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_schedule ALTER COLUMN date_create SET NOT NULL;

  D_SCHEDULE:
    Колонка: LPU
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN lpu SET NOT NULL;

    Колонка: CODE
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN code SET NOT NULL;

    Колонка: ID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN id SET NOT NULL;

    Колонка: QUOTING
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN quoting SET NOT NULL;

    Колонка: NAME
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN name SET NOT NULL;

    Колонка: CID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_schedule ALTER COLUMN cid SET NOT NULL;

