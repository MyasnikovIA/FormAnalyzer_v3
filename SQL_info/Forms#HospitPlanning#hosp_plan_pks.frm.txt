----------------------------------------------------------------------------------------------------
ФОРМА: Forms/HospitPlanning/hosp_plan_pks.frm
----------------------------------------------------------------------------------------------------
Базовая форма: C:\AppServ\www\5_mis_MEDDEV-151210\Forms\HospitPlanning\hosp_plan_pks.frm
СТАТУС: БАЗОВАЯ ФОРМА

ЮЗЕРФОРМЫ:
     (не найдено)

SubForm:
     (не найдено)

Список вызываемых форм в JS:
     HospitPlanning/hpk_pk_quotes.frm

Коды подключаемого AutoPopUp меню на форме:
        HPK_PAYMENT_KINDS;


Контекстное меню используемое на форме (ПКМ) – данные из OracleSQL:

└── name="pHPK_PAYMENT_KINDS"
    ├── "Обновить"
    ├── "Добавить"
    ├── "Удалить"
    └── "Квоты"

SQL ЗАПРОСЫ (1):
     (содержимое скрыто)

ИСПОЛЬЗУЕМЫЕ ТАБЛИЦЫ И ВЬЮХИ:
    D_V_HPK_PAYMENT_KINDS

ТАБЛИЦЫ, ИСПОЛЬЗУЕМЫЕ ЧЕРЕЗ ВЬЮХИ (уникальные для этой формы):
    D_HPK_PAYMENT_KINDS

ДЕТАЛЬНОЕ СОДЕРЖИМОЕ ВЬЮХ (таблицы):

  D_V_HPK_PAYMENT_KINDS (Oracle: 1,25 тыс) (PostgreSQL: 541):
          D_HPK_PAYMENT_KINDS (Oracle: 1,34 тыс) (PostgreSQL: 1,34 тыс)

JS Unit Compositions (только из вызовов openWindow/openD3Form):
    unit:PAYMENT_KIND  composition:LIST;

БРОКЕРЫ:
    unit:HPK_PAYMENT_KINDS  action:INSERT;
    unit:HPK_PAYMENT_KINDS  action:DELETE;
    unit:HPK_PAYMENT_KINDS  action:UPDATE;

ПРОВЕРКА ПЕРВИЧНЫХ КЛЮЧЕЙ (Oracle vs PostgreSQL):

  D_HPK_PAYMENT_KINDS:
    Статус: ОШИБКА: PK есть в Oracle, но отсутствует в PostgreSQL
    Oracle PK поля: ID

ПРОВЕРКА NOT NULL CONSTRAINT (Oracle vs PostgreSQL):

  D_HPK_PAYMENT_KINDS:
    Колонка: LPU
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN lpu SET NOT NULL;

    Колонка: PAYMENT_KIND
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN payment_kind SET NOT NULL;

    Колонка: HAS_QUOTES
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN has_quotes SET NOT NULL;

    Колонка: PID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN pid SET NOT NULL;

    Колонка: ID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN id SET NOT NULL;

    Колонка: CID
      Oracle: NOT NULL
      PostgreSQL: NULL разрешен
      ОШИБКА: В Oracle NOT NULL, в PostgreSQL NULL разрешен
      Рекомендация: ALTER TABLE d_hpk_payment_kinds ALTER COLUMN cid SET NOT NULL;

